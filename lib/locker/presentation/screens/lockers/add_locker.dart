import 'package:flutter/material.dart';

class AddLocker extends StatelessWidget {
  const AddLocker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add Locker"),
    );
  }
}
